# ==============================================================================
# Receiptly Backend - Docker Environment Configuration
# ==============================================================================
# Copy this file to .env and update the values for your deployment

# Database Configuration
POSTGRES_PASSWORD=your-secure-database-password-change-this
POSTGRES_PORT=5432

# Backend Configuration
BACKEND_PORT=8000
DEBUG=false

# CORS Configuration (Add your frontend URLs)
CORS_ORIGINS=http://localhost:3000,http://your-frontend-domain.com,https://your-frontend-domain.com

# Authentication Configuration
# Generate a secure secret key with: openssl rand -hex 32
SECRET_KEY=your-very-secure-secret-key-change-this-in-production-make-it-long-and-random-use-openssl-rand-hex-32

# Caddy Configuration
# Backend will be accessible on BACKEND_PORT for Caddy to proxy
# Configure your Caddyfile to proxy to http://localhost:8000

# ==============================================================================
# Development vs Production Settings
# ==============================================================================
# For development, you can set DEBUG=true and use simpler passwords
# For production, ensure:
# 1. DEBUG=false
# 2. Strong POSTGRES_PASSWORD
# 3. Cryptographically secure SECRET_KEY
# 4. Proper CORS_ORIGINS for your domain
# 5. Use HTTPS in production